- name: Install and Run Nginx
  hosts: web
  
  tasks: 
  - name: install Nginx
    ansible.builtin.package:
     name: Nginx
     state: present

  - name: run Nginx
    ansible.builtin.service:
     name: Nginx
     state: started
     enabled: yes
